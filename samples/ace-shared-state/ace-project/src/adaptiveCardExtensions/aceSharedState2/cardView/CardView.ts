import {
  BaseBasicCardView,
  IBasicCardParameters,
  IExternalLinkCardAction,
  IQuickViewCardAction,
  ICardButton
} from '@microsoft/sp-adaptive-card-extension-base';
import * as strings from 'AceSharedState2AdaptiveCardExtensionStrings';
import { IAceSharedState2AdaptiveCardExtensionProps, IAceSharedState2AdaptiveCardExtensionState, QUICK_VIEW_REGISTRY_ID } from '../AceSharedState2AdaptiveCardExtension';

export class CardView extends BaseBasicCardView<IAceSharedState2AdaptiveCardExtensionProps, IAceSharedState2AdaptiveCardExtensionState> {
  public get cardButtons(): [ICardButton] | [ICardButton, ICardButton] | undefined {
    return [
      {
        title: strings.QuickViewButton,
        action: {
          type: 'QuickView',
          parameters: {
            view: QUICK_VIEW_REGISTRY_ID
          }
        }
      }
    ];
  }

  public get data(): IBasicCardParameters {
    return {
      primaryText: this.state.description
    };
  }

  public get onCardSelection(): IQuickViewCardAction | IExternalLinkCardAction | undefined {
    return {
      type: 'ExternalLink',
      parameters: {
        target: 'https://www.bing.com'
      }
    };
  }
}
